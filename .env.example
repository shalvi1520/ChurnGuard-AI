# ============================================
# ChurnGuard Frontend – Environment Variables
# ============================================
# Copy this file to .env and fill in values as needed.
#
# ChurnGuard needs the real backend running to do anything (upload a
# dataset, train, predict, explain) — see backend/README or DEVELOPMENT.md
# for how to start it (uvicorn backend.api.main:app --reload).

# --- Vite (client-side, exposed to the browser — never put secrets here) ---

# Base URL of the FastAPI + ML backend (backend/).
VITE_API_BASE_URL=http://localhost:8000/api
